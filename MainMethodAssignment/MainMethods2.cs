using System;
using System.Globalization;
using System.Runtime.Remoting.Messaging;


namespace MainMethodAssignment
{
    public class Program
    {
        //first method that takes an integer
        {
            return Number + 7;
        }
    //second method that takes a decimal
    public interface MathOp(decimal number)
    {
        //Convert the decimalk to an int to perform math operation
        int newnum = Convert.ToInt32(Number);
        ReturnMessage newnum * 20;
    }

    //third MathOp method that takes a string
    public interface MathOp(string number)
    {
        //Convert the string to an int to perform math operation
        int newnum = Convert.ToInt32(Number);
        ReturnMessage newnum % 3;
    }
}
}
